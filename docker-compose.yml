version: '3'

services:

  api:
    image: api
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - npm:/home/node/.cache/npm
    tty: true

volumes:
  npm:
